<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Notification Test Center"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Test different notification types"
            android:textSize="14sp"
            android:textColor="@color/text_secondary"
            android:layout_marginBottom="24dp" />

        <!-- New Reservation Test -->
        <Button
            android:id="@+id/btnTestNewReservation"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Test New Reservation"
            android:textColor="@color/white"
            android:backgroundTint="@color/primary"
            android:layout_marginBottom="12dp" />

        <!-- Update Test -->
        <Button
            android:id="@+id/btnTestUpdate"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Test Reservation Update"
            android:textColor="@color/white"
            android:backgroundTint="@color/status_seated_text"
            android:layout_marginBottom="12dp" />

        <!-- Reminder Test -->
        <Button
            android:id="@+id/btnTestReminder"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Test Upcoming Reminder"
            android:textColor="@color/white"
            android:backgroundTint="@color/status_upcoming_text"
            android:layout_marginBottom="12dp" />

        <!-- Cancellation Test -->
        <Button
            android:id="@+id/btnTestCancellation"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Test Cancellation"
            android:textColor="@color/white"
            android:backgroundTint="@color/status_noshow_text"
            android:layout_marginBottom="24dp" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/text_secondary"
            android:layout_marginBottom="24dp" />

        <!-- Clear All -->
        <Button
            android:id="@+id/btnClearAll"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Clear All Notifications"
            android:textColor="@color/text_primary"
            android:backgroundTint="@color/background"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

        <!-- Instructions -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Instructions:"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginTop="32dp"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1. Click any button to test notifications\n2. Swipe down to see notifications\n3. Click notification to open details\n4. FCM token is logged to console"
            android:textSize="14sp"
            android:textColor="@color/text_secondary"
            android:lineSpacingExtra="4dp" />

    </LinearLayout>
</ScrollView>